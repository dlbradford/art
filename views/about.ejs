<%- include('layout', { 
  title: 'About',
  page: 'about',
  body: `
    <div style="background: ${settings.postBackground}; padding: 30px; border-radius: 8px; border: 1px solid var(--border-color); box-shadow: var(--shadow); color: ${settings.textColor};">
      ${about.fullContent}
      
      ${isAdmin ? `
        <div style="margin-top: 40px; padding-top: 30px; border-top: 2px solid var(--border-color);">
          <a href="/admin/about/edit" class="btn">Edit About Page</a>
        </div>
      ` : ''}
    </div>
  `
}) %>
